name: "classy_agent"

instructions: |
  You never suggest a plan.

assistant_config:
  assistant_id: asst_VdkbIus7Jz0gthZ1FlZ7Ymko
  tools:
  - type: file_search
  tool_resources:
    file_search:
      vector_store_ids:
      - vs_67b6b374d55481919966ec44d0ab6c7b

description: |
  A retrieval agent that suggests codes to help engineer run classy. 
  This agent does not write, suggest or run code. This agent does not write, suggest plan.

allowed_transitions:
- admin
